[project]
name = "certflow-learning-agent"
version = "0.1.0"
description = "UiPath coded Python agent that generates certification learning paths for CertFlow."
authors = [{ name = "CertFlow Team" }]
requires-python = ">=3.11"
dependencies = [
  "uipath>=2.0",
]
